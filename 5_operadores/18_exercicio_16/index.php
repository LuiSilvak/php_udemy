<?php

    echo "Olá usuário" . "<br>";

    $x = 5;
    $y = 3;

# Utilizando a Igualdade
    if ($x == $y) {
        echo "$x é igual a $y" . "<br>";
    }

# Utilizando a Diferença
    if ($x != $y) {
        echo "$x é diferente de $y" . "<br>";
    }

# Utilizando o Idêntico
    if ($x === $y) {
        echo "$x é idêntico a $y" . "<br>";
    }

# Utilizando o Não idêntico
    if ($x !== $y) {
        echo "$x não é idêntico a $y" . "<br>";
    }

